<div class="row justify-content-center mt-5">
    <div class="col-md-5">
        <div class="card border-danger shadow">
            <div class="card-body text-center p-5">
                <h2 class="text-danger mb-4">Are you sure?</h2>
                <p>You are about to delete <strong><%= phone.name %></strong>. This cannot be undone.</p>
                <form action="/phones/delete/<%= phone._id %>" method="post" class="mt-4">
                    <button type="submit" class="btn btn-danger btn-lg me-2">Yes, Delete</button>
                    <a href="/phoneList" class="btn btn-secondary btn-lg">No, Keep it</a>
                </form>
            </div>
        </div>
    </div>
</div>